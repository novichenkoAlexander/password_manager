<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.example.passwordmanager.screens.main.MainFragment"
        android:label="MainFragment"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/toNoteFragment"
            app:destination="@id/NoteFragment" />
    </fragment>

    <fragment
        android:id="@+id/NoteFragment"
        android:name="com.example.passwordmanager.screens.note.NoteFragment"
        android:label="NoteFragment"
        tools:layout="@layout/fragment_note">
        <argument
            android:name="note"
            app:argType="com.example.passwordmanager.models.Note"
            app:nullable="true" />

    </fragment>
</navigation>